<%= form_with(model: tour_photo, local: true) do |form| %>
  <% if tour_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tour_photo.errors.count, "error") %> prohibited this tour_photo from being saved:</h2>

      <ul>
      <% tour_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
