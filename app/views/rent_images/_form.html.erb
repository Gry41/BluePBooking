<%= form_with(model: rent_image, local: true) do |form| %>
  <% if rent_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rent_image.errors.count, "error") %> prohibited this rent_image from being saved:</h2>

      <ul>
      <% rent_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
