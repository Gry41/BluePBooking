<br>
<div class="container">
  <div class="row">
    <div class="col s12 m6">
      
          <div class="card">
<div class="container">
<br>
<%= form_with(model: cartype, local: true) do |form| %>
  <% if cartype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cartype.errors.count, "error") %> prohibited this cartype from being saved:</h2>

      <ul>
      <% cartype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-field">
    
    <%= form.text_field :name, id: "cartype_name" %>
    <label for="cartype_name" class="active">Car type</label>
  </div>

  <div class="actions">
    <%= form.submit class:"waves-effect waves-light btn" %>
  </div>
  <br>
<% end %>
</div></div></div></div></div>