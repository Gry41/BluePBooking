<%= form_with(model: taxi_photo, local: true,:html=>{ :multipart => true }) do |form| %>
  <% if taxi_photo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(taxi_photo.errors.count, "error") %> prohibited this taxi_photo from being saved:</h2>

      <ul>
      <% taxi_photo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <strong>Add Photo:</strong>
  <div class="btn-floating btn-large waves-effect waves-light" style="font-size:25px;" id="btn-circular">+</div>
   <%= file_field_tag "image[]", type: :file, multiple:true , style:"display:none;"%>

  <div class="actions">
    <%= form.submit class:"waves-effect waves-light btn"%>
  </div>
<% end %>
